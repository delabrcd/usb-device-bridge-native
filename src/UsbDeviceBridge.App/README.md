# UsbDeviceBridge.App

Non-elevated desktop client for the native rewrite.

Implementation target: WinUI 3 application using gRPC to talk to UsbDeviceBridge.Service.

This project is intentionally scaffold-first while the .NET SDK and WinUI templates are being prepared on this machine.
